const bcrypt = require("bcrypt-nodejs");
const User = require("../models/user");
//const jwt = require("")

function signUp(req, res) {
  console.log("An Endpoint of signUp");
}

module.exports = {
  signUp
};
